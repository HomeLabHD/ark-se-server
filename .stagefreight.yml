# ------------------------------------------------------------------------------
# GENERATED / ENFORCED BY STAGEFREIGHT GOVERNANCE
# Source repo: PrPlanIT/MaintenancePolicy
# Source ref: d2c8f0a57f07f41ea5666565027da2812e4072c5
# Profile: hlhd-docker
# This .stagefreight.yml is governed, not purely local.
# To disable governance, detach from the control repo workflow.
# Manual changes may be overwritten by reconciliation.
# ------------------------------------------------------------------------------

version: 1
vars:
  github_org: HomeLabHD
  github_repo: ark-se-server
  gitlab_group: PrPlanIT/HomeLabHD
  org: hlhd
  repo: ark-se-server
builds:
  preset: preset/build-docker.yml
scribe:
  preset: preset/scribe-docker.yml
lint:
  preset: preset/lint-generic.yml
security:
  preset: preset/security.yml
dependency:
  preset: preset/dependency.yml
commit:
  preset: preset/commit.yml
release:
  preset: preset/release.yml
manifest:
  preset: preset/manifest.yml
build_cache:
  preset: preset/build_cache.yml
ci:
  forge:
    - gitlab
  image: docker.cr.pcfae.com/prplanit/stagefreight:latest-dev
forges:
  preset: preset/forges.yml
git:
  preset: preset/git.yml
metadata:
  category: games
  description:
    - 'Dedicated server image for ARK: Survival Evolved, for single instances and clusters.'
    - 'Container image running an ARK: Survival Evolved dedicated server, supporting both standalone instances and multi-server clusters, built on the SteamCMD base image.'
  license: MIT
  org: HomeLabHD
  title: 'ARK: Survival Evolved Server'
  topics:
    - ark
    - game-server
    - steamcmd
    - dedicated-server
    - gaming
orgs:
  HomeLabHD:
    aliases:
      docker: hlhd
      ghcr: homelabhd
      github: HomeLabHD
      handle: hlhd
    maintainer: HomeLabHD <homelabhelp@gmail.com>
preset_source:
  cache_policy: authoritative
  project_id: PrPlanIT/MaintenancePolicy
  provider: gitlab
  ref: d2c8f0a57f07f41ea5666565027da2812e4072c5
  repo_url: https://gitlab.prplanit.com
publish:
  preset: preset/publish-docker.yml
registries:
  preset: preset/registries.yml
repos:
  github-mirror:
    forge: github
    project: HomeLabHD/ark-se-server
    roles:
      - mirror
      - publish-origin
    sync:
      git: true
      releases: true
  primary:
    branches:
      default: main
    forge: gitlab
    project: HomeLabHD/ark-se-server
    roles:
      - primary
    worktree: .
stencils:
  presets:
    - preset/stencils-base.yml
    - preset/stencils-status.yml
    - preset/stencils-docker.yml
toolchains:
  preset: preset/toolchains.yml
